<div class="ui mini menu massive inverted">
  <%= link_to 'MusicWorld', root_path, class: 'active item' %>

    <%=link_to 'Контакты', contacts_path, class:'item' %>

  <a class="item">
    Информация
  </a>
  <a class="item">
    Об услугах
  </a>

    <%= link_to "Units", unit_index_path, class: ' item' %>


    <%=link_to 'Отзывы', pages_reviews_path, class: 'item'%>

  <div class="right menu">
    <% unless user_signed_in? %>
      <div class="item">
        <%= link_to "Sign Up", new_user_registration_path, class: 'ui primary button' %>
      </div>
      <div class="item">
        <%= link_to "Sign In", new_user_session_path, class: 'ui primary button' %>
      </div>
    <% else %>
      <div class="item">
        <% if current_user.admin? %>
          <h1><%=link_to '[Admin] '+current_user.email.split('@')[0], profile_update_path %></h1>
        <% else %>
          <h1><%=link_to current_user.email.split('@')[0], profile_update_path %></h1>
        <% end %>
      </div>
      <div class="item">
        <%= link_to "Sign Out", destroy_user_session_path, :method => :delete, class: 'ui primary button' %>
      </div>
    <% end %>
  </div>
</div>